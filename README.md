# game-cheats

Central cheat database for the simple-cheat-menu PS3 plugin. Consoles download their game's
compiled cheat file from here and report back whether cheats worked, building a per-game
reliability score over time.

## Layout

```
source/<TITLEID>.txt     the human layer: one editable file per game + aliases for its variants
compiled/<TITLEID>.txt   the console layer: one standalone file per serial, generated - never edit
votes/...                raw console feedback, merged by the validation workflow
tools/                   seeder (one-time import), compile (source -> compiled), reconcile (votes -> scores)
```

A game's regional/PSN variants share one source file: the primary serial holds the cheats, every
other serial is an alias:

```
# God of War: Ascension
# DO NOT EDIT THIS FILE - all variants share BCES01742.txt, edit that instead
same-as: BCES01742
```

(The `same-as:` line is the machine-readable pointer used by the compiler and the PR validator;
the comment is for you.) The compiler materializes a full standalone `compiled/` copy for every
serial, so consoles always fetch exactly one file with no indirection.

## Cheat file format

```
# God of War: Ascension

name: Infinite Health
score BCES01742 01.12: 88 13+ 1-
mode: once
aob 3C800004... 4863277C...
w32 0066AB20 6C656400
```

- `w8/w16/w32 <address> <value>` pokes and `aob <find> <replace>` search-patches.
- `score <titleId> <version>: <score> <confirms>+ <fails>-` lines are written by tooling from
  console feedback - don't edit them; they show where the cheat is proven.
- Cheats have no stored ids: identity is a hash of the code lines, computed by tools and consoles.

## Contributing

Edit (or create) `source/<TITLEID>.txt` for the serial you know. If it says DO NOT EDIT, follow
the `same-as:` pointer and edit that file instead. For a brand-new game, start the file with a
`# Game Title` line. New cheats (pure additions) merge automatically after validation; edits to
existing cheats are welcome too and get human-reviewed before merging. Never touch `compiled/` -
it is regenerated from `source/`.

## How the feedback loop works

- **Consoles read** `compiled/<their serial>.txt` directly (one conditional GET).
- **Consoles report** anonymously: AoB pattern found/missing (automatic) and works/broken
  (user-pressed) land as empty files under `votes/` via auto-validated pull requests. No console
  ids, no accounts - nothing links two votes to the same machine.
- **Reconcile** (run by the maintainer or a workflow) folds votes into the `score` lines in
  `source/`, recompiles, and prunes the queue. Scores are advisory: untested is shown as unknown,
  never as broken.
